# Review package: bb03b3f..c1ef2d0

## Commits
c1ef2d0 feat: add AvailableSeries model and ConversionStatus enum

## Files changed
 src/VsiConverter/VsiConverter.UI/Models/AvailableSeries.cs   | 12 ++++++++++++
 src/VsiConverter/VsiConverter.UI/Models/ConversionStatus.cs   | 13 +++++++++++++
 2 files changed, 25 insertions(+)

## Diff
diff -U10 bb03b3f..c1ef2d0
─── /dev/null
+++ b/src/VsiConverter/VsiConverter.UI/Models/AvailableSeries.cs
@@ -0,0 +1,12 @@
+namespace VsiConverter.UI.Models;
+
+public record AvailableSeries(
+    int Index,
+    int Width,
+    int Height,
+    double PixelSizeX,
+    string? Description)
+{
+    public override string ToString()
+        => $"Series {Index}: {Width}x{Height}{(Description is not null ? $" ({Description})" : "")}";
+}
─── /dev/null
+++ b/src/VsiConverter/VsiConverter.UI/Models/ConversionStatus.cs
@@ -0,0 +1,13 @@
+namespace VsiConverter.UI.Models;
+
+public enum ConversionStatus
+{
+    Queued,
+    CheckingCompanion,
+    DetectingSeries,
+    Converting,
+    Zipping,
+    Completed,
+    Failed,
+    Cancelled
+}
