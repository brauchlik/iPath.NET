<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:VsiConverter.UI.ViewModels"
        x:Class="VsiConverter.UI.Views.SettingsDialog"
        x:DataType="vm:SettingsViewModel"
        Title="Settings"
        Width="500" Height="400"
        WindowStartupLocation="CenterOwner">
  <StackPanel Margin="16" Spacing="12">
    <TextBlock Text="Compression Quality" FontWeight="Bold" />
    <Grid ColumnDefinitions="*,Auto">
      <Slider Minimum="50" Maximum="100" TickFrequency="5"
              Value="{Binding CompressionQuality}" />
      <TextBlock Grid.Column="1" Text="{Binding CompressionQuality, StringFormat='{}{0}%'}"
                 Width="40" TextAlignment="Center" />
    </Grid>

    <TextBlock Text="Toolchain" FontWeight="Bold" Margin="0,8,0,0" />
    
    <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="8">
      <StackPanel Spacing="6">
        <TextBlock Text="{Binding StatusText}" Foreground="Gray" />

        <Grid ColumnDefinitions="Auto,*,Auto">
          <TextBlock Text="Java:" Width="75" VerticalAlignment="Center" />
          <TextBlock Grid.Column="1" Text="{Binding JavaVersion}" VerticalAlignment="Center" />
          <Button Grid.Column="2" x:Name="BtnBrowseJava" Content="Browse" Width="70" />
        </Grid>

        <Grid ColumnDefinitions="Auto,*,Auto">
          <TextBlock Text="bfconvert:" Width="75" VerticalAlignment="Center" />
          <TextBlock Grid.Column="1" Text="{Binding BfconvertPath}" VerticalAlignment="Center" />
          <Button Grid.Column="2" x:Name="BtnBrowseBfconvert" Content="Browse" Width="70" />
        </Grid>

        <Grid ColumnDefinitions="Auto,*,Auto">
          <TextBlock Text="vips:" Width="75" VerticalAlignment="Center" />
          <TextBlock Grid.Column="1" Text="{Binding VipsPath}" VerticalAlignment="Center" />
          <Button Grid.Column="2" x:Name="BtnBrowseVips" Content="Browse" Width="70" />
        </Grid>
      </StackPanel>
    </Border>

    <StackPanel Orientation="Horizontal" Spacing="8">
      <Button x:Name="BtnCheck" Content="Check Tools" />
      <Button x:Name="BtnDownloadBf" Content="Download bfconvert" />
      <Button x:Name="BtnDownloadVips" Content="Download vips" />
    </StackPanel>

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,16,0,0">
      <Button x:Name="BtnClose" Content="Close" />
    </StackPanel>
  </StackPanel>
</Window>
